<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lpg.com.mapper.TSysEmailMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="lpg.com.pojo.TSysEmail">
        <id column="id" property="id" />
        <result column="receivers_email" property="receiversEmail" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="send_user_id" property="sendUserId" />
        <result column="send_user_name" property="sendUserName" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, receivers_email, title, content, send_user_id, send_user_name, create_time
    </sql>

</mapper>
